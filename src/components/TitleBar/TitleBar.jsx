import React from "react";
import './TitleBar.css';

function TitleBar(props){
    return(
        <div className="title-bar">
            <h1>Dom's Library</h1>
        </div>
    );
}

export default TitleBar;